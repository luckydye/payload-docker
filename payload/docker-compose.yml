version: "3.9"

services:
  payload:
    build: .
    ports:
      - 3000:3000
    environment:
      - MONGODB_URI=mongodb://db/admin
      - MONGODB_USER=payload
      - MONGODB_PASSWORD=payload123
      - PAYLOAD_SECRET=OAIJd9ajsdoimaSOIDJASDj
  db:
    image: mongo:5.0
    ports:
      - 27017:27017
    volumes:
      - ~/apps/mongo:/data/db
    environment:
      - MONGO_INITDB_DATABASE=payload
      - MONGO_INITDB_ROOT_USERNAME=payload
      - MONGO_INITDB_ROOT_PASSWORD=payload123
